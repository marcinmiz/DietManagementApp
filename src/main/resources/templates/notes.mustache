<!DOCTYPE html>
<html xmlns:=http://www.w3.org/1999/xhtml>
<head>
	{{> meta}}
	<title>Konto Użytkownika</title>
</head>
<body>
<section class="container">
	<div class="my-3" align="center">
		<h3>Witaj, {{#user}}{{name}}{{/user}}</h3>
		<form action="/logout" method="post">
			<input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
			<input class="button" type="submit" value="Wyloguj"/>
		</form>
	</div>
	<div class="my-3" align="center">
		<form action="/addnote" method="post">
			<input type="text" name="title" placeholder="Nazwa produktu">
<!--            <input type="text" name="product" placeholder="Nazwa">-->
            <input type="text" name="note" placeholder="Opis">
			<input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
			<input class="button" type="submit" value="+">
		</form>
	</div>
	{{#notes}}
    <div class="mt-3">
        <div>
            <h2>{{title}}</h2>
        </div>
        <div>
            {{note}}
        </div>
    </div>
    {{/notes}}
</section>
{{^notes}}
<section class="container">
	 Nie ma jeszcze żadnych produktów.
</section>
{{/notes}}
</body>
</html>